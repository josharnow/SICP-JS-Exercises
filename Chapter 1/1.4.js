// Adds the first operand to the absolute value of the second operand.
// If b >= 0, the function invokes the plus function because b is already positive
// If b < 0, the function invokes the minus function to invert b to its positive absolute value